---
category: Primitives
---

# Input

Text input field, styled with the design-system tokens. Accepts all native `<input>` props
(`type`, `placeholder`, `value`, `disabled`, …). Set `aria-invalid` to show the error state.

```tsx
<Input type="email" placeholder="Enter your email" />
<Input aria-invalid />
```
